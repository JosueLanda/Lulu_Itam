#include <stdio.h>

int main()
{

    int cota_repeticiones, iterador;

    scanf("%d", &cota_repeticiones);

    for (iterador = 0; iterador < cota_repeticiones; iterador++)
    {
        printf("FES ACATLAN ES TU SEGUNDA CASA\n");
    }
    return 0;
}